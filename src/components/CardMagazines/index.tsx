import { Card, Title } from "./styles";

export type Props = {
    magazine: string,
    link: string,
    image: string
}

export default function CardMagazine({magazine, link, image}: Props) {
    return(
        <Card>
            
        </Card>
    );
}